# 25 端到端时序图与泳道图

## 1. 文档目标

本文件定义“用户输入小说构思，到输出可播放章节视频”的端到端时序，明确：

- 谁发起任务
- 谁决定阶段流转
- 哪些阶段占用 GPU
- 哪些阶段可以并行
- 哪些阶段必须等人工确认
- 失败后回到哪里重跑

---

## 2. 主要参与者

- `web-ui`：前端任务中心
- `api-gateway`：统一入口
- `orchestrator`：状态机与编排器
- `asset-store`：文件与产物索引
- `llm-worker`：小说/脚本/提示词重写
- `tts-worker`：旁白/对白音频生成
- `image-worker`：关键帧生成
- `video-worker`：I2V 视频生成
- `qc-worker`：ASR、时长校验、质量评分
- `compose-worker`：字幕、混音、mux、导出
- `review-console`：人工审核页面

---

## 3. 章节生产总时序

```mermaid
sequenceDiagram
    actor U as User
    participant UI as Web UI
    participant API as API Gateway
    participant O as Orchestrator
    participant A as Asset Store
    participant L as LLM Worker
    participant T as TTS Worker
    participant I as Image Worker
    participant V as Video Worker
    participant Q as QC Worker
    participant C as Compose Worker
    participant R as Review Console

    U->>UI: 提交构思/风格/目标时长
    UI->>API: POST /projects/{id}/episodes/generate
    API->>O: create_job(ep01)
    O->>L: 生成 story bible / chapter / shot script
    L-->>O: script_bundle.json
    O->>A: 保存文本类产物

    par 音频支线
      O->>T: 生成 narrator/dialogue 音频
      T-->>O: wav + timing
      O->>A: 保存音频与元数据
    and 图像支线
      O->>I: 生成角色关键帧/镜头关键帧
      I-->>O: keyframes + prompt_meta
      O->>A: 保存图像与元数据
    end

    O->>Q: 文本/音频/关键帧预检
    Q-->>O: precheck_report

    alt 需要人工确认
      O->>R: 创建 review task
      R-->>O: approve / reject / patch
    end

    O->>V: 逐镜头 I2V 生成
    loop each shot
      V-->>O: shot_video.mp4
      O->>Q: 视频质量检查
      Q-->>O: shot_score + defects
      alt shot 不合格
        O->>V: 重试或切换模板
      end
    end

    O->>C: 合成对白/旁白/BGM/字幕/视频
    C-->>O: episode_master.mp4
    O->>Q: 终检
    Q-->>O: final_report

    alt 通过
      O->>A: 标记发布版本
      O-->>API: completed
      API-->>UI: 展示下载与回放链接
    else 未通过
      O->>R: 请求人工处理
    end
```

---

## 4. 泳道图：单镜头生产

```mermaid
flowchart LR
    subgraph 用户/前端
        A1[选择镜头] --> A2[提交重跑参数]
    end

    subgraph 编排器
        B1[读取 shot spec] --> B2[读取依赖产物]
        B2 --> B3[构造执行上下文]
    end

    subgraph 图像层
        C1[选择关键帧] --> C2[补充 motion prompt]
    end

    subgraph 视频层
        D1[申请 GPU 锁] --> D2[加载 I2V 模型]
        D2 --> D3[生成 shot video]
        D3 --> D4[释放 GPU 锁]
    end

    subgraph 质检层
        E1[解析时长] --> E2[抽帧评分]
        E2 --> E3[返回评分报告]
    end

    A2 --> B1 --> C1 --> D1 --> E1
```

---

## 5. 并行与串行规则

### 5.1 串行阶段

以下阶段必须通过 GPU 串行执行：

- LLM 长文本生成（当使用 GPU 推理）
- TTS 推理（若启用 GPU）
- 关键帧扩散生成
- I2V 视频生成
- ASR 若采用 GPU 大模型

### 5.2 可并行阶段

以下阶段允许 CPU 并行：

- shot script 切分
- prompt 结构化与模板展开
- 目录创建与元数据写入
- FFmpeg 素材准备
- 日志写入与评分聚合
- review 任务创建

### 5.3 批处理建议

- 同一章节的 `dialogue` 音频可以按角色分组批量生成
- 同一场景的关键帧可以按 `scene_id` 成组生成
- 视频必须按 `shot_id` 逐个执行，不建议多镜头并行争抢 GPU

---

## 6. 人工介入点

建议只保留三个明确的人工审核点：

1. **脚本审核点**：确认剧情方向、节奏和台词
2. **关键帧审核点**：确认角色脸和场景风格
3. **终片审核点**：确认是否导出正式版本

除此之外，默认走自动流水线，不在中间增加过多人工 stop。

---

## 7. 失败回退路径

| 失败阶段 | 回退起点 | 是否需要重跑前序 | 说明 |
|---|---|---|---|
| story bible 失败 | concept_input | 否 | 仅重试 LLM |
| shot script 不满意 | chapter_text | 否 | 可重新适配为脚本 |
| TTS 读错 | 单句 audio segment | 否 | 定位片段重生 |
| 关键帧风格漂移 | 单镜头 keyframe | 否 | 替换参考图后重生 |
| I2V 闪烁/畸变 | 单镜头 shot_video | 否 | 切模板或重写 motion prompt |
| 合成失败 | compose task | 否 | 重新跑 ffmpeg |
| 终检不过 | 失败镜头集合 | 否 | 只回滚问题镜头 |

---

## 8. 章节级状态主图

```mermaid
stateDiagram-v2
    [*] --> Created
    Created --> Drafting
    Drafting --> ScriptReady
    ScriptReady --> AudioGenerating
    ScriptReady --> KeyframeGenerating
    AudioGenerating --> AudioReady
    KeyframeGenerating --> KeyframeReady
    AudioReady --> Precheck
    KeyframeReady --> Precheck
    Precheck --> ReviewPending
    ReviewPending --> Approved
    Approved --> ShotVideoGenerating
    ShotVideoGenerating --> ShotVideoReady
    ShotVideoReady --> Composing
    Composing --> FinalQC
    FinalQC --> Published
    FinalQC --> ReviewPending
    Published --> [*]
```

---

## 9. 实现要求

- 所有阶段必须输出统一 `stage_result.json`
- 每次状态变化必须写入 `job_events`
- 任何人工修改都要产生新版本，不覆盖旧版本
- `shot_id` 是最小重跑粒度，不能直接只重跑“半个镜头”
- 所有 worker 都必须支持 `dry_run=true` 模式，用于联调
