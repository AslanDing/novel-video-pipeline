# Audio Generation Plan：音频生成改造计划 (M3) - ✅ 已集成

## 改造目标 ✅ 已完成并集成
将当前的 `stage3_audio` 中的长段朗读，切换为"解析 `script_x.jsonl`，按不同角色音色去单行生成并获取音频真正时长"的切片机制。

---

## 改造步骤 - 完成状态

### 1. 结构化入口与音色配置映射 ✅ 已完成并集成
在 `tts_engine.py` 中已集成 **TTSScriptAdapter**：

- ✅ **已实现并集成**: `TTSScriptAdapter` 类 - 读取 `script_x.jsonl`
- ✅ **已集成**: 在 `_generate_from_script()` 方法中使用 TTSScriptAdapter
- ✅ **已实现**: `load_script_lines()` - 加载分镜脚本
- ✅ **已实现**: `get_voice_segments()` - 提取需要生成音频的分段
- ✅ **已实现**: `check_existing_audio()` - 检查音频是否已存在（断点续传）

```python
# tts_engine.py:725-737
async def process(self, novel: Novel) -> Dict[int, ChapterAudio]:
    """优先从 script_x.jsonl 加载"""
    # 尝试从 script_x.jsonl 加载
    chapter_audio = await self._generate_from_script(novel_title, chapter.number)
    # 如果没有脚本文件，回退到原来的方式
    if not chapter_audio or not chapter_audio.segments:
        chapter_audio = await self._generate_chapter_audio(chapter)

    # 生成 timeline_manifest.json
    await self._save_timeline_manifest(novel_title, chapter.number, chapter_audio)
```

### 2. 逐行分片与 API/本地模型请求 ✅ 已完成
- ✅ **已实现**: 防重复机制（断点机制）
- ✅ **已实现**: Edge TTS 合成接口
- ⚠️ **部分实现**: ChatTTS / GPT-SoVITS 框架存在但功能有限

### 3. 时长写入 (Duration Update) ✅ 已完成并集成
在 `tts_engine.py` 中已集成 **TimelineGenerator**：

- ✅ **已实现并集成**: `TimelineGenerator` 类 - 生成 timeline_manifest.json
- ✅ **已集成**: 在 `_save_timeline_manifest()` 方法中调用
- ✅ **已实现**: `create_timeline()` - 创建时间线清单，包含真实时长

```python
# tts_engine.py:852-879
async def _save_timeline_manifest(self, novel_title: str, chapter_number: int, chapter_audio: ChapterAudio):
    """保存时间线清单"""
    from stages.stage3_audio.tts_script_adapter import TimelineGenerator

    adapter = TTSScriptAdapter(novel_title, data_dir, audio_dir)
    script_lines = adapter.load_script_lines(chapter_number)

    generator = TimelineGenerator(audio_dir)
    manifest = generator.create_timeline(chapter_number, script_lines, audio_files)
    generator.save_timeline(manifest, chapter_number)

    print(f"   📜 已生成 timeline_ch{chapter_number:03d}.json")
```

### 4. 容错检查 ✅ 已完成
- ✅ **已实现**: 文本清理函数 `clean_text_for_tts()` 处理非法发音字符
- ✅ **已实现**: 错误处理，不卡死

---

## 关键技术实现状态

| 功能 | 实现文件 | 完成度 | 说明 |
|------|---------|--------|------|
| Edge-TTS 支持 | `tts_engine.py` | ✅ 95% | 完整实现 |
| 文本清理 | `tts_engine.py` | ✅ 95% | 完整规则 |
| 角色自动映射 | `tts_engine.py` | ✅ 75% | 智能匹配 |
| **TTSScriptAdapter** | `tts_engine.py` | ✅ 已集成 | 读取 script_x.jsonl |
| **TimelineGenerator** | `tts_engine.py` | ✅ 已集成 | 生成 timeline_manifest.json |
| ChatTTS 框架 | `tts_engine.py` | ⚠️ 50% | 框架存在 |
| GPT-SoVITS 框架 | `tts_engine.py` | ⚠️ 40% | 框架存在 |
| Fish/CosyVoice | - | ❌ 30% | 仅配置占位 |
| 音频后期处理 | `audio_post_processor.py` | ✅ 90% | 完整实现 |
| BGM 自动匹配 | - | ❌ 50% | 库存在，未自动匹配 |
| SFX 自动添加 | - | ❌ 50% | 库存在，未自动添加 |

---

## 流程验证

### 数据流
```
Stage 1: script_x.jsonl
    ↓
Stage 3: TTSEngine.process()
    → _generate_from_script() 使用 TTSScriptAdapter
        → load_script_lines() 读取 script_x.jsonl
        → get_voice_segments() 提取分段
        → 逐行生成音频
    → _save_timeline_manifest() 使用 TimelineGenerator
        → create_timeline() 生成时间线
        → 保存 timeline_chXX.json
    ↓
输出: audio/voices/*.wav + timeline_chXX.json
```

---

## 下游 Stage 集成状态

| Stage | 使用 timeline_manifest.json | 集成状态 |
|-------|---------------------------|----------|
| Stage 4 (视频) | ✅ | 已集成 |

---

## 后续任务

### P0 - 必须完成
(无)

### P1 - 应该完成
1. **Fish Audio / CosyVoice 集成**: 实现实际的 API 调用
2. **BGM/SFX 自动匹配**: 根据场景情感自动添加背景音乐和音效

### P2 - 优化项
3. **音频后期自动处理**: 在合成后自动应用响度标准化、EQ

---

## 关联文档

- [实现差距分析](7.implement_gap.md)
- [图像生成计划](9.image_generation_plan.md)
- [视频生成计划](11.video_generation_plan.md)

---

*文档版本: 1.3*
*最后更新: 2026-03-30*
*状态: ✅ 已完成并集成*